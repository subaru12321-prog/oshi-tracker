# .env としてコピーしてから、実際の値を入れてください。
# cp .env.example .env
# このファイルは絶対にGitにコミットしない・他人と共有しないこと。

# TikTok(ローカル実行専用。クラウドでは収集しません)。
# ブラウザのCookieから ms_token を手動で取得して入れる。
# 取得方法: ブラウザでtiktok.comにログイン→開発者ツール→Application→Cookies→ms_token の値をコピー
TIKTOK_MS_TOKEN=
