# スマホの通知をダッシュボードに転送する設定 (Instagram / X / TikTok)

Instagram・X・TikTokの投稿は、GitHubのサーバーからは取得できません
(データセンターのIPが各アプリ側にブロックされているため)。そこで代わりに、
**普段お使いのスマホにその場で届く「新しい投稿をしました」通知を横取りして、
GitHubに転送する**という方法を使います。

この方法で分かるのは「どのメンバーが」「どのアプリに」「いつ投稿したか」
と、アプリを開くためのリンクだけです。**投稿の本文や画像そのものは
取得できません**(通知にはそこまで詳しい情報が含まれていないためです)。
それでも「投稿があったこと」にすぐ気づけるようになります。

作業はスマホ(Android)だけで完結します。パソコンでの作業はありません。
所要時間は30分〜1時間くらいを見ておいてください。

## 全体の流れ

```
Instagram/X/TikTokアプリが通知を出す
   ↓
MacroDroid(スマホの自動化アプリ)がその通知に気づく
   ↓
MacroDroidがGitHubにインターネット経由でデータを送る
   ↓
GitHub Actionsが記録して、ダッシュボードに反映される
   ↓
MacroDroidが元の通知を消す(通知欄を汚さないため)
```

以下の手順1〜5を順番にやってください。手順6は最後の動作確認です。

---

## 手順1: 各アプリで「投稿の通知」をONにする

推しメンバーのアカウントをフォローしていても、標準では「投稿するたびに通知」
という設定にはなっていないことが多いです。フォローしている各メンバーの
プロフィール画面で、通知をONにしてください。

**注意:** アプリのバージョンやお使いの端末によって、ボタンの位置や名前が
多少変わることがあります。以下は目安です。見当たらない場合は、プロフィール
画面の「⋮」(点3つ)メニューの中も探してみてください。

- **Instagram**: メンバーのプロフィール画面を開く → 「フォロー中」ボタンの
  近くにある**鈴(ベル)のアイコン**をタップ → 「すべて」または「投稿」を
  選ぶ。
- **X (旧Twitter)**: メンバーのプロフィール画面を開く → 「フォロー中」
  ボタンの近くにある**鈴のアイコン**をタップ → 「すべてのポスト」を選ぶ。
- **TikTok**: メンバーのプロフィール画面を開く → 「⋮」(点3つ、または
  ベルのアイコン)をタップ → 「投稿の通知をオンにする」を選ぶ。

これを、ダッシュボードで追いたいメンバー全員×3アプリぶん繰り返します。

---

## 手順2: 通知はサイレントにする(消すのではない)

ここが一番間違えやすいポイントなので、先に注意点だけまとめます。

> **絶対にやってはいけないこと: 通知そのものをOFF/ブロックにする**
>
> Androidは、通知が「ブロックされている」アプリについては、そもそも
> 通知を作りません。通知が作られなければMacroDroidも気づけないので、
> この仕組み全体が動かなくなります。「通知がうるさいから」といって
> Instagram/X/TikTokの通知をアプリ単位でOFFにしないでください。

うるさくしたくない場合は、OFFではなく**「サイレント」**にします。
サイレントにすると、音・振動・画面上のポップアップ(ヘッドアップ通知)は
出なくなりますが、通知そのものはこれまで通り作られるので、MacroDroidは
問題なく気づけます。

設定方法(Androidの設定アプリから):

1. **設定 → アプリ → (Instagram / X / TikTok) → 通知** を開く。
2. 投稿に関する通知チャンネル(「投稿」「新しい投稿」など、アプリにより
   名前が違います)をタップする。
   - **どのチャンネル名が該当するかはアプリのバージョンによって変わるため、
     確認が必要です。** 該当しそうなものが複数あれば、念のため関連しそうな
     チャンネルをまとめてサイレントにしておくと安全です。
3. 「重要度」を**サイレント**に変更する(音・バイブ・ポップアップがすべて
   OFFになる設定です)。「通知を表示しない」「オフ」ではないことを確認して
   ください。

これで、通知欄には一瞬通知が現れますが、無音・無振動になります。
そしてこの後設定するMacroDroidが、GitHubへの転送が終わり次第すぐに
その通知を消してくれるので、実際には通知欄はほぼ汚れません。

なお、Androidに**通知履歴**という機能があり(設定 →
通知 → 通知履歴、端末により場所が違います)、これをONにしておくと、
MacroDroidが消した後の通知も履歴として振り返れます。「本当に転送できて
いるか」を後から確認したいときに便利なので、有効にしておくのがおすすめです。

---

## 手順3: MacroDroidをインストールして権限を許可する

1. Google PlayストアでMacroDroidを検索してインストールする(無料版で
   問題ありません。詳しくは手順4末尾の補足を参照)。
2. 初回起動時に、いくつか権限の許可を求められます。その中で
   **「通知へのアクセス」**(Notification Access)は必ず許可してください。
   これがないと、そもそも他アプリの通知の中身を読み取れません。
   - 求められなかった場合は、MacroDroidの設定画面から
     手動で「通知へのアクセス」を有効にできます。Androidの設定アプリ側で
     「MacroDroid」への通知アクセスをONにする画面が開くので、そこで
     許可してください。
3. バッテリー最適化の対象からMacroDroidを外すよう案内されることもあります。
   案内が出たら従ってください(省電力機能でMacroDroidが止められると、
   通知に気づけなくなることがあるためです)。

---

## 手順4: GitHubのトークンを発行する

MacroDroidがGitHubにデータを送るには、GitHub側の「合鍵」となる
アクセストークンが必要です。

1. ブラウザ(スマホでもパソコンでもOK)でgithub.comにログインする。
2. 右上のアイコン → **Settings** → 左メニュー下の方の
   **Developer settings** → **Personal access tokens** →
   **Fine-grained tokens** → **Generate new token**。
3. 設定項目:
   - **Repository access**: 「Only select repositories」を選び、
     `oshi-tracker` リポジトリだけを選択する。
   - **Permissions → Repository permissions → Contents**:
     「Read and write」に変更する。
   - 有効期限(Expiration)はお好みで(短くすると、期限が来た時点で
     再発行が必要になります)。
4. **Generate token** を押すと、`github_pat_...` から始まる長い文字列が
   一度だけ表示されます。

> **重要: このトークンはパスワードと同じ、絶対に他人に見せてはいけない
> 情報です。**
> - チャット(Claudeとの会話も含む)に貼り付けない。
> - 誰にも共有しない。
> - ファイルに保存してリポジトリにコミットしない。
> - 使い道は、次の手順5でMacroDroidの設定画面に直接貼り付けることだけです。
>
> もし誤って人に見せてしまったり、どこかにアップロードしてしまった場合は、
> GitHubのトークン一覧画面からそのトークンを **Delete** して無効化して
> ください。

トークンをコピーしたら、手順5に進みます(この画面を閉じるとトークンは
二度と表示されないので、コピーできているか確認してから閉じてください)。

**MacroDroidの無料版について:** 無料版は広告が表示され、作成できる
マクロが5個までという制限がありますが、HTTP通信を行うアクション自体は
無料版でも使えます(公式wikiで確認済み)。この設定では下記の手順5で
マクロを1個作るだけなので、無料版のままで問題なく完結します。

---

## 手順5: マクロを作る

MacroDroidを開き、**マクロを追加**(Add Macro)をタップして、以下の
内容を作ります。3つのアプリを1個のマクロにまとめられるので、マクロは
**1個だけ**で構いません。

### トリガー: 通知 (Notification)

1. トリガーを追加 → **Notification** を選ぶ。
2. Trigger Event は **Notification Received** のまま。
3. Application Selection で **Select Applications** を選び、
   Instagram・X・TikTokの3つにチェックを入れる(Include)。
4. Text Filtering は **Any text** のままでよい(投稿の内容で絞り込む
   必要はありません)。
5. そのまま保存する。

### アクション1: HTTP Request (通知をGitHubに転送する)

1. アクションを追加 → **HTTP Request** を選ぶ。
2. **Request method**: `POST`
3. **URL**:
   ```
   https://api.github.com/repos/subaru12321-prog/oshi-tracker/dispatches
   ```
4. **Header Params**(キーと値を1組ずつ追加する欄です。以下の3組を
   追加してください):
   | Key | Value |
   |---|---|
   | `Authorization` | `Bearer` の後ろに半角スペース1つ空けて、手順4で
     コピーしたトークンを貼り付ける(例: `Bearer github_pat_xxxxxxxx`) |
   | `Accept` | `application/vnd.github+json` |
   | `Content-Type` | `application/json` |
5. **Content Body**: Content Typeを `application/json` にして、
   本文(テキスト)に以下をそのまま入力する。
   ```
   {"event_type":"notification","client_payload":{"app":"{not_app_name}","title":"{not_title}","text":"{notification}"}}
   ```
   `{not_app_name}`・`{not_title}`・`{notification}` の部分は、
   MacroDroidの「Magic text」という機能で、実際に届いた通知の中身に
   自動的に置き換わります。手入力ではなく、MacroDroidのテキスト入力欄に
   ある「変数を挿入」的なボタン(欄の横にあるアイコン)から選んで挿入する
   方法もあります(そちらの方が打ち間違いがなく安全です)。
   - `{not_app_name}` = 通知を出したアプリ名(Instagram / X / TikTok)
   - `{not_title}` = 通知のタイトル(投稿者のユーザー名が入ることが多い)
   - `{notification}` = 通知の本文メッセージ
   - これら3つの変数名は、MacroDroid公式wiki(Trigger: Notification
     のページ)で確認済みの正式名称です。
6. **Block next action until complete** を **ON** にする。
   (これをONにしておかないと、通信が終わる前に次のアクション(通知を
   消す処理)が走ってしまう可能性があります。)
7. 保存する。

### アクション2: 通知を消す(Clear Notifications)

1. アクションを追加 → **Clear Notifications** を選ぶ。
2. **Use Notification Trigger** を選ぶ(このトリガーで拾った、まさに
   その通知だけを消す設定です)。
3. 保存する。

### 順番の確認

アクションの並び順は上から下に実行されるので、必ず

1. HTTP Request(先に転送する)
2. Clear Notifications(転送してから消す)

の順にしてください。逆にすると、通知を消してから転送しようとすることに
なり、うまく動かない可能性があります。

最後にマクロ全体に名前(例:「SNS通知転送」)を付けて保存すれば完成です。

---

## 手順6: 動作確認

1. 実際に、フォローしているメンバーの誰かが新しく投稿するのを待つか、
   もし可能であれば別の(自分の)アカウントで試しに投稿してみて、
   通知が来るのを確認する。
2. 通知が来たら、数秒〜十数秒後に通知欄からその通知が消えることを確認する
   (= MacroDroidが転送してから消した、という合図です)。
3. ダッシュボード(GitHub Pagesのページ、またはリポジトリの
   `docs/data.json`)を開き、その投稿が記録されているか確認する。
   すぐに反映されないときは、リポジトリの **Actions** タブを開いて
   **notify** ワークフローが実行されているか、エラーが出ていないかを見る。

### うまくいかないときのチェックリスト

- **通知自体が来ない**
  → 手順1(アプリ側の投稿通知)がONになっているか確認する。
  → 手順2で誤って通知をOFF/ブロックにしていないか確認する(サイレントは
    OK、OFFはNG)。
- **通知は来るがMacroDroidが反応しない**
  → MacroDroidに「通知へのアクセス」権限が許可されているか確認する。
  → マクロ自体がONになっているか(マクロ一覧でスイッチが入っているか)
    確認する。
  → バッテリー最適化でMacroDroidが止められていないか確認する。
- **MacroDroidは動いているのにダッシュボードに出ない**
  → HTTP Requestの **URL**・**Header Params**・**Content Body** を
    見直す。特にトークンの `Bearer` の後ろのスペースや、貼り付けたトークン
    に余分な改行・空白が入っていないか確認する。
  → トークンの有効期限が切れていないか、`oshi-tracker` リポジトリへの
    Contents(Read and write)権限が付いているか、GitHubのトークン一覧
    画面で確認する。
  → リポジトリの **Actions** タブで `notify` ワークフローがそもそも
    起動しているかを確認する。起動していないなら、`event_type` が
    `notification` になっているか(ワークフロー側の設定と一致している
    必要があります)、リポジトリ名・ユーザー名のスペルを見直す。
  → MacroDroidの「System Log」(システムログ)機能で、HTTP Requestの
    実行結果(ステータスコードやエラー内容)を確認できます。うまくいかない
    ときはまずここを見るのがおすすめです。

---

## 補足: 今回確認できたこと・できなかったこと

- MacroDroid公式wiki(wiki.macrodroid.com)を実際に参照し、Notification
  トリガーのMagic text変数名(`{not_title}`・`{notification}`・
  `{not_app_name}` など)、HTTP Requestアクションの項目名(Header Params /
  Content Body)、Clear Notificationsアクションの「Use Notification
  Trigger」オプション、無料版が5マクロまでという制限を確認済みです。
- 一方で、Instagram/X/TikTok側の通知ON設定の正確なボタン位置・名前、
  Androidの通知チャンネル名は、アプリのバージョンやOSのバージョンで
  変わるため、実際の画面を見ながら近いものを探す必要があります
  (**確認が必要**な部分として本文中にも明記しています)。
